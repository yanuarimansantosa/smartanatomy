import { PageStub } from "@/components/page-stub";

export default function TenantsPage() {
  return (
    <PageStub
      title="Tenants"
      subtitle="Provision klinik / dokter praktek, atur subdomain, isolasi database, dan paket modul per-tenant."
      cta={{ label: "Provision tenant pertama" }}
    />
  );
}
