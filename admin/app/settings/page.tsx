import { PageStub } from "@/components/page-stub";

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      subtitle="Konfigurasi platform — brand default, tier pricing, SATUSEHAT OAuth credentials (platform-level)."
    />
  );
}
