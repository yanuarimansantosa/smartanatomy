import { PageStub } from "@/components/page-stub";

export default function SecurityPage() {
  return (
    <PageStub
      title="Security"
      subtitle="IAM, session, tenant isolation (Postgres RLS), data-at-rest encryption keys (KEK), dan audit log policy."
    />
  );
}
