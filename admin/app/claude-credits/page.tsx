import { PageStub } from "@/components/page-stub";

export default function ClaudeCreditsPage() {
  return (
    <PageStub
      title="Claude credits"
      subtitle="Reseller pool untuk embedded AI assistant di web builder tenant. Atur free allocation, daily/monthly reset, dan top-up."
      badge="NEW"
      cta={{ label: "Atur free allocation" }}
    />
  );
}
