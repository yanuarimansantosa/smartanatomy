import { PageStub } from "@/components/page-stub";

export default function NotificationsPage() {
  return (
    <PageStub
      title="Notifications"
      subtitle="Konfigurasi channel notifikasi platform: Resend (email), Telegram bot. WhatsApp Business Account ditunda V2."
    />
  );
}
