import { PageStub } from "@/components/page-stub";

export default function ActivityPage() {
  return (
    <PageStub
      title="Activity log"
      subtitle="Immutable audit log lintas tenant — setiap aksi admin, perubahan konfigurasi, dan event sistem."
    />
  );
}
